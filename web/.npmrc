# Build scripts for sharp and unrs-resolver are SKIPPED (not run) via
# ignoredBuiltDependencies in pnpm-workspace.yaml. approve-builds is a no-op
# here since the workspace config already suppresses the postinstall prompts.
approve-builds=true
