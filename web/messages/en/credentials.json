{
  "title": "Credentials",
  "description": "Write-only secrets injected into MCP server requests. Secrets are never displayed.",
  "add": "Add credential",
  "readOnlyTooltip": "Read-only while viewing as another user",
  "table": {
    "server": "Server",
    "injectHeader": "Inject header",
    "allowedHosts": "Allowed hosts",
    "actions": "Actions",
    "hostCount": "{count, plural, one {# host} other {# hosts}}",
    "none": "—"
  },
  "actions": {
    "update": "Update",
    "delete": "Delete"
  },
  "empty": {
    "description": "No credentials yet. Add one to enable secret injection for a server."
  },
  "deleteDialog": {
    "title": "Delete credential for \"{server}\"?",
    "description": "The stored secret will be permanently removed.",
    "confirm": "Delete"
  },
  "dialog": {
    "addTitle": "Add credential",
    "updateTitle": "Update credential — {server}",
    "description": "Secrets are write-only. Saving replaces the existing secret; nothing is ever displayed.",
    "save": "Save credential",
    "saving": "Saving…"
  },
  "form": {
    "server": {
      "label": "Server name",
      "placeholder": "e.g. my-server",
      "hint": "Pattern: <code>{pattern}</code>"
    },
    "secret": {
      "label": "Secret",
      "placeholder": "Paste your secret here…"
    },
    "advanced": {
      "toggle": "Advanced (transitional)",
      "note": "These fields will eventually be sourced from the manifest."
    },
    "injectHeader": {
      "label": "Inject header",
      "placeholder": "Authorization",
      "hint": "Pattern: <code>{pattern}</code>, max 64 chars."
    },
    "injectFormat": {
      "label": "Inject format",
      "placeholder": "Bearer '{token}'",
      "hint": "Must contain <code>'{token}'</code>, max 256 chars."
    },
    "placeholder": {
      "label": "Placeholder",
      "placeholder": "REPLACE_ME",
      "hint": "8–256 chars."
    },
    "allowedHosts": {
      "label": "Allowed hosts",
      "placeholder": "api.example.com\n*.other.com",
      "hint": "One host per line (or comma-separated). Each up to 253 chars, pattern <code>{pattern}</code>."
    }
  },
  "toast": {
    "saved": "Credential for \"{server}\" saved",
    "deleted": "Credential for \"{server}\" deleted",
    "deleteFailed": "Delete failed"
  },
  "errors": {
    "saveFailed": "Failed to save credential"
  }
}
